window.onload = () => {
    //Delaying the display time.

    setTimeout(() => {

        const nameEl = document.getElementById('my-name');
        const name = "olatunji babajide";

        nameEl.innerText = name;
    }, 1000);
}